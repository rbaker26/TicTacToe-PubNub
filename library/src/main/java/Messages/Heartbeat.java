package Messages;

/**
 * This represents a heartbeat. Its main significance is that it exists,
 * showing that the sender is still alive.
 */
public class Heartbeat {
    public static final long DEFAULT_BEAT_PERIOD = 10 * 1000;
}
